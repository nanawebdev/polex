import React from "react";

export default function UiTitle({text}) {
    return (
        <div style={{
            fontFamily: "Patrick Hand', cursive"
            }}>
            {text}    
        </div>
    )
}